# lcj-fares Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Codziennie zbierać kalendarz cen Ryanair z/do Łodzi (LCJ) na 12 miesięcy do przodu, trzymać historię jako dziennik zmian w CSV i publikować analizę (tanie terminy + „kiedy kupować”) na GitHub Pages.

**Architecture:** Pakiet Pythona `lcjfares` z czterema modułami o jednej odpowiedzialności: `ryanair` (HTTP + parsowanie), `store` (pliki CSV/JSON), `collect` (snapshot → zmiany), `analyze` (historia → `site/data.json`). Statyczna strona `site/index.html` czyta `data.json` i rysuje wykresy Chart.js. GitHub Actions uruchamia collect → analyze → commit → deploy Pages raz dziennie.

**Tech Stack:** Python 3.12, `curl_cffi==0.15.0` (impersonacja Chrome), `pytest>=8`, Chart.js 4.4.1 z jsDelivr, GitHub Actions + GitHub Pages.

**Spec:** `docs/superpowers/specs/2026-09-25-lcj-fares-design.md`

## Global Constraints

- Tylko Ryanair; endpoint cen: `https://www.ryanair.com/api/farfnd/v4/oneWayFares/{O}/{D}/cheapestPerDay?outboundMonthOfDate=YYYY-MM-01&currency=PLN`.
- Trasy: dynamicznie z `https://www.ryanair.com/api/views/locate/searchWidget/routes/en/airport/LCJ`, obie strony (LCJ→X i X→LCJ).
- Horyzont: bieżący miesiąc + 11 kolejnych (12 miesięcy). Waluta PLN, 1 dorosły.
- `data/prices.csv` kolumny dokładnie: `observed,origin,dest,day,dep_time,price,status`; `status ∈ {ok, unavailable, soldout}`; `price` = `"%.2f"` albo puste.
- `data/runs.csv` kolumny dokładnie: `observed,started_utc,requests,failed,status,routes,missing`; `status ∈ {ok, partial, failed}`. (Kolumna `routes` = `;`-lista kierunków próbowanych w danym runie — dodana względem specu, potrzebna do rozróżnienia „nie próbowano” od „bez zmian”.)
- Wiersz w `prices.csv` tylko przy zmianie `(dep_time, price, status)` dla klucza `(origin, dest, day)`.
- Do 3 prób na zapytanie, rosnący odstęp; pauza 0,5–1 s między zapytaniami.
- `failed * 2 > requests` ⇒ `status=failed` i kod wyjścia 1.
- Próg małej próby `MIN_N = 5`. Koszyki dni do wylotu: `0-7, 8-14, 15-30, 31-60, 61-90, 91-180, 181+`.
- Cron `0 6 * * *` (UTC), `concurrency: collect`.
- Repo publiczne `areqq/lcj-fares` (konto `gh`: `areqq`). Utworzenie repo na GitHubie wymaga potwierdzenia użytkownika w momencie wykonania.
- Katalog projektu: `/home/areq/lcj-fares` (już istnieje, `git init` zrobiony, spec zacommitowany).

## Review Focus

1. **Przeszłe dni bieżącego miesiąca** — API zwraca je jako `unavailable`; nie mogą trafić do `prices.csv` (inaczej każdy lot „znika” tuż po wylocie w sposób mylący analizę). Test: Task 3 `test_first_run_writes_future_days_only`.
2. **Lot wyprzedany/niedostępny, który wcześniej był tani** — nie może się pokazać w „najtańszych nadchodzących”. Test: Task 4 `test_build_full` (lot 10-08 soldout wykluczony).
3. **Luka w pomiarze** (miesiąc w `missing` albo kierunek nieobecny w `routes` runu) — nie wolno jej traktować jak „cena bez zmian”. Testy: Task 4 `test_reconstruct_respects_missing`, `test_reconstruct_skips_route_not_attempted`.
4. **Pusta historia / pierwszy run nieudany** — `analyze` musi wygenerować poprawny `data.json`, strona pokazać „brak danych”, a nie wywalić się. Test: Task 4 `test_build_empty`; Task 5 ręczna weryfikacja.
5. **Ponowne uruchomienie tego samego dnia** (ręczny `workflow_dispatch` po cronie) — brak zdublowanych wierszy cen. Test: Task 3 `test_rerun_same_day_adds_no_price_rows`.

---

## File Structure

```
lcj-fares/
  pyproject.toml            konfiguracja pytest (pythonpath)
  requirements.txt          zależności (CI + lokalnie)
  .gitignore
  README.md                 opis, uruchamianie lokalne, link do strony
  lcjfares/__init__.py      pusty
  lcjfares/ryanair.py       HTTP z retry, parse_fares, routes_from, cheapest_per_day
  lcjfares/store.py         CSV prices/runs, routes.json, last_state
  lcjfares/collect.py       months_ahead, directions, diff_rows, run, main
  lcjfares/analyze.py       bucket_label, reconstruct, build, main
  site/index.html           strona (heatmapa, krzywa, tabela)
  tests/fixtures/cheapest_LCJ_STN_2026-11.json   prawdziwa odpowiedź API
  tests/test_ryanair.py
  tests/test_store.py
  tests/test_collect.py
  tests/test_analyze.py
  .github/workflows/collect.yml
  data/                     (tworzone przez collect)
```

---

### Task 1: Szkielet projektu + klient Ryanair

**Files:**
- Create: `pyproject.toml`, `requirements.txt`, `.gitignore`, `lcjfares/__init__.py`, `lcjfares/ryanair.py`
- Create: `tests/fixtures/cheapest_LCJ_STN_2026-11.json` (pobrane z API)
- Test: `tests/test_ryanair.py`

**Interfaces:**
- Consumes: nic.
- Produces:
  - `class ApiError(Exception)`
  - `class Fare(NamedTuple): day: str; dep_time: str; price: str; status: str` — `day` = `YYYY-MM-DD`, `dep_time` = `HH:MM` lub `""`, `price` = `"%.2f"` lub `""`, `status` ∈ `ok|unavailable|soldout`
  - `get_json(url: str, params: dict | None = None, *, retries: int = 3, backoff: float = 2.0, sleep=time.sleep) -> dict | list` — rzuca `ApiError`
  - `parse_fares(data: dict) -> list[Fare]` — rzuca `ApiError` przy nieznanym schemacie
  - `cheapest_per_day(origin: str, dest: str, month: str, *, sleep=time.sleep) -> list[Fare]` — `month` = `YYYY-MM-01`
  - `routes_from(origin: str, *, sleep=time.sleep) -> list[str]`

- [ ] **Step 1: Pliki konfiguracyjne i środowisko**

`pyproject.toml`:
```toml
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
```

`requirements.txt`:
```
curl_cffi==0.15.0
pytest>=8
```

`.gitignore`:
```
.venv/
__pycache__/
.pytest_cache/
```

`lcjfares/__init__.py`: pusty plik.

Run:
```bash
cd /home/areq/lcj-fares && python3 -m venv .venv && .venv/bin/pip install -q -r requirements.txt
```
Expected: instalacja bez błędów.

- [ ] **Step 2: Pobierz fixture z prawdziwego API**

Run:
```bash
cd /home/areq/lcj-fares && mkdir -p tests/fixtures && .venv/bin/python -c "
import curl_cffi, json
r = curl_cffi.get('https://www.ryanair.com/api/farfnd/v4/oneWayFares/LCJ/STN/cheapestPerDay',
                  params={'outboundMonthOfDate': '2026-11-01', 'currency': 'PLN'}, impersonate='chrome131', timeout=30)
r.raise_for_status()
open('tests/fixtures/cheapest_LCJ_STN_2026-11.json', 'w').write(json.dumps(r.json(), indent=1, ensure_ascii=False))
print(len(r.json()['outbound']['fares']))"
```
Expected: `30`

- [ ] **Step 3: Write the failing tests**

`tests/test_ryanair.py`:
```python
import json
from pathlib import Path

import pytest

from lcjfares import ryanair
from lcjfares.ryanair import ApiError, Fare

FIX = Path(__file__).parent / "fixtures"


def _day(day, **kw):
    base = {"day": day, "arrivalDate": None, "departureDate": None, "price": None,
            "soldOut": False, "unavailable": False}
    base.update(kw)
    return base


def test_parse_fares_real_fixture():
    data = json.loads((FIX / "cheapest_LCJ_STN_2026-11.json").read_text())
    fares = ryanair.parse_fares(data)
    raw = data["outbound"]["fares"]
    assert len(fares) == len(raw) == 30
    assert all(f.status in {"ok", "unavailable", "soldout"} for f in fares)
    ok_raw = next(r for r in raw if not r["unavailable"] and not r["soldOut"])
    ok = next(f for f in fares if f.day == ok_raw["day"])
    assert ok.price == f"{ok_raw['price']['value']:.2f}"
    assert ok.dep_time == ok_raw["departureDate"][11:16]
    assert ok.status == "ok"


def test_parse_fares_statuses():
    data = {"outbound": {"fares": [
        _day("2026-11-01", departureDate="2026-11-01T13:40:00", price={"value": 394.2}),
        _day("2026-11-02", unavailable=True),
        _day("2026-11-03", departureDate="2026-11-03T09:05:00", soldOut=True),
    ]}}
    assert ryanair.parse_fares(data) == [
        Fare("2026-11-01", "13:40", "394.20", "ok"),
        Fare("2026-11-02", "", "", "unavailable"),
        Fare("2026-11-03", "09:05", "", "soldout"),
    ]


@pytest.mark.parametrize("data", [
    {"foo": 1},
    {"outbound": {"fares": [{"day": "2026-11-01"}]}},
    {"outbound": {"fares": [_day("2026-11-01", price=None)]}},
    [],
])
def test_parse_fares_rejects_unknown_schema(data):
    with pytest.raises(ApiError):
        ryanair.parse_fares(data)


def test_get_json_retries_then_succeeds(monkeypatch):
    calls, sleeps = [], []

    def fake(url, params):
        calls.append(url)
        if len(calls) < 3:
            raise RuntimeError("429")
        return {"ok": True}

    monkeypatch.setattr(ryanair, "_http_get", fake)
    assert ryanair.get_json("u", sleep=sleeps.append) == {"ok": True}
    assert len(calls) == 3
    assert sleeps == [2.0, 4.0]


def test_get_json_gives_up_after_retries(monkeypatch):
    calls = []

    def fake(url, params):
        calls.append(url)
        raise RuntimeError("403")

    monkeypatch.setattr(ryanair, "_http_get", fake)
    with pytest.raises(ApiError):
        ryanair.get_json("u", sleep=lambda s: None)
    assert len(calls) == 3


def test_routes_from(monkeypatch):
    monkeypatch.setattr(ryanair, "_http_get", lambda url, params: [
        {"arrivalAirport": {"code": "STN"}}, {"arrivalAirport": {"code": "DUB"}}])
    assert ryanair.routes_from("LCJ", sleep=lambda s: None) == ["STN", "DUB"]


def test_routes_from_rejects_unknown_schema(monkeypatch):
    monkeypatch.setattr(ryanair, "_http_get", lambda url, params: {"x": 1})
    with pytest.raises(ApiError):
        ryanair.routes_from("LCJ", sleep=lambda s: None)
```

- [ ] **Step 4: Run tests to verify they fail**

Run: `cd /home/areq/lcj-fares && .venv/bin/pytest tests/test_ryanair.py -q`
Expected: FAIL — `ImportError: cannot import name 'ryanair'`

- [ ] **Step 5: Implement `lcjfares/ryanair.py`**

```python
"""Klient otwartego API fare-finder Ryanair (bez auth). Na bazie skyskaner/ryanair_client.py."""
from __future__ import annotations

import time
from typing import NamedTuple

import curl_cffi

W = "https://www.ryanair.com/api"
_UA = "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 Chrome/131 Safari/537.36"
_FARE_KEYS = {"day", "departureDate", "price", "soldOut", "unavailable"}


class ApiError(Exception):
    """Zapytanie nieudane po wszystkich próbach albo odpowiedź w nieznanym formacie."""


class Fare(NamedTuple):
    day: str        # YYYY-MM-DD
    dep_time: str   # HH:MM albo ""
    price: str      # "%.2f" albo ""
    status: str     # ok | unavailable | soldout


def _http_get(url, params):
    r = curl_cffi.get(url, params=params, headers={"User-Agent": _UA},
                      impersonate="chrome131", timeout=30)
    r.raise_for_status()
    return r.json()


def get_json(url, params=None, *, retries=3, backoff=2.0, sleep=time.sleep):
    last = None
    for attempt in range(retries):
        try:
            return _http_get(url, params)
        except Exception as e:  # sieć, HTTP 4xx/5xx, zły JSON — wszystko ponawiamy
            last = e
            if attempt < retries - 1:
                sleep(backoff * 2 ** attempt)
    raise ApiError(f"{url}: {last}") from last


def parse_fares(data) -> list[Fare]:
    try:
        raw = data["outbound"]["fares"]
    except (KeyError, TypeError) as e:
        raise ApiError(f"brak outbound.fares: {e!r}") from e
    out = []
    for f in raw:
        if not isinstance(f, dict) or not _FARE_KEYS <= f.keys():
            raise ApiError(f"nieoczekiwany format dnia: {f!r}"[:200])
        dep = (f["departureDate"] or "")[11:16]
        if f["unavailable"]:
            out.append(Fare(f["day"], "", "", "unavailable"))
        elif f["soldOut"]:
            out.append(Fare(f["day"], dep, "", "soldout"))
        else:
            try:
                price = f"{float(f['price']['value']):.2f}"
            except (KeyError, TypeError, ValueError) as e:
                raise ApiError(f"brak ceny dla {f['day']}: {e!r}") from e
            out.append(Fare(f["day"], dep, price, "ok"))
    return out


def cheapest_per_day(origin, dest, month, *, sleep=time.sleep) -> list[Fare]:
    """Najtańszy lot każdego dnia miesiąca (month = YYYY-MM-01)."""
    data = get_json(f"{W}/farfnd/v4/oneWayFares/{origin}/{dest}/cheapestPerDay",
                    {"outboundMonthOfDate": month, "currency": "PLN"}, sleep=sleep)
    return parse_fares(data)


def routes_from(origin, *, sleep=time.sleep) -> list[str]:
    data = get_json(f"{W}/views/locate/searchWidget/routes/en/airport/{origin}", sleep=sleep)
    try:
        return [r["arrivalAirport"]["code"] for r in data]
    except (KeyError, TypeError) as e:
        raise ApiError(f"nieoczekiwany format tras: {e!r}") from e
```

- [ ] **Step 6: Run tests to verify they pass**

Run: `cd /home/areq/lcj-fares && .venv/bin/pytest tests/test_ryanair.py -q`
Expected: PASS (wszystkie testy)

- [ ] **Step 7: Commit**

```bash
cd /home/areq/lcj-fares && git add pyproject.toml requirements.txt .gitignore lcjfares tests && git commit -m "feat: Ryanair fare-finder client with retries and schema checks"
```

---

### Task 2: Magazyn plików (`store`)

**Files:**
- Create: `lcjfares/store.py`
- Test: `tests/test_store.py`

**Interfaces:**
- Consumes: nic.
- Produces:
  - `PRICE_FIELDS = ["observed", "origin", "dest", "day", "dep_time", "price", "status"]`
  - `RUN_FIELDS = ["observed", "started_utc", "requests", "failed", "status", "routes", "missing"]`
  - `read_prices(path: Path) -> list[dict[str, str]]`, `read_runs(path: Path) -> list[dict[str, str]]` — brak pliku ⇒ `[]`
  - `append_prices(path: Path, rows: list[dict]) -> None` — pusta lista ⇒ nic (plik nie powstaje)
  - `append_run(path: Path, row: dict) -> None`
  - `last_state(rows: list[dict]) -> dict[tuple[str, str, str], tuple[str, str, str]]` — `(origin, dest, day) -> (dep_time, price, status)`, ostatni wiersz wygrywa
  - `load_routes(path: Path) -> list[str]` (brak pliku ⇒ `[]`), `save_routes(path: Path, routes: list[str]) -> None`

- [ ] **Step 1: Write the failing tests**

`tests/test_store.py`:
```python
from lcjfares import store


def row(observed, day, price, status="ok", dep="10:00", origin="LCJ", dest="STN"):
    return {"observed": observed, "origin": origin, "dest": dest, "day": day,
            "dep_time": dep, "price": price, "status": status}


def test_read_missing_file_returns_empty(tmp_path):
    assert store.read_prices(tmp_path / "prices.csv") == []
    assert store.read_runs(tmp_path / "runs.csv") == []


def test_append_prices_roundtrip_header_once(tmp_path):
    p = tmp_path / "data" / "prices.csv"
    store.append_prices(p, [row("2026-09-25", "2026-11-01", "394.24")])
    store.append_prices(p, [row("2026-09-26", "2026-11-01", "", "soldout")])
    rows = store.read_prices(p)
    assert rows == [row("2026-09-25", "2026-11-01", "394.24"),
                    row("2026-09-26", "2026-11-01", "", "soldout")]
    assert p.read_text().splitlines()[0] == ",".join(store.PRICE_FIELDS)
    assert p.read_text().count("observed") == 1


def test_append_empty_does_not_create_file(tmp_path):
    p = tmp_path / "prices.csv"
    store.append_prices(p, [])
    assert not p.exists()


def test_append_run_roundtrip(tmp_path):
    p = tmp_path / "runs.csv"
    r = {"observed": "2026-09-25", "started_utc": "2026-09-25T06:00:00Z", "requests": 168,
         "failed": 1, "status": "partial", "routes": "DUB;STN", "missing": "LCJ-STN-2026-11"}
    store.append_run(p, r)
    assert store.read_runs(p) == [{k: str(v) for k, v in r.items()}]


def test_last_state_latest_row_wins():
    rows = [row("2026-09-25", "2026-11-01", "394.24"),
            row("2026-09-25", "2026-11-02", "100.00"),
            row("2026-09-27", "2026-11-01", "299.00")]
    assert store.last_state(rows) == {
        ("LCJ", "STN", "2026-11-01"): ("10:00", "299.00", "ok"),
        ("LCJ", "STN", "2026-11-02"): ("10:00", "100.00", "ok"),
    }


def test_routes_roundtrip(tmp_path):
    p = tmp_path / "routes.json"
    assert store.load_routes(p) == []
    store.save_routes(p, ["STN", "AGP"])
    assert store.load_routes(p) == ["AGP", "STN"]
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /home/areq/lcj-fares && .venv/bin/pytest tests/test_store.py -q`
Expected: FAIL — `ImportError: cannot import name 'store'`

- [ ] **Step 3: Implement `lcjfares/store.py`**

```python
"""Pliki danych: dziennik zmian cen (prices.csv), log uruchomień (runs.csv), lista tras (routes.json)."""
from __future__ import annotations

import csv
import json
from pathlib import Path

PRICE_FIELDS = ["observed", "origin", "dest", "day", "dep_time", "price", "status"]
RUN_FIELDS = ["observed", "started_utc", "requests", "failed", "status", "routes", "missing"]


def _read(path: Path) -> list[dict]:
    if not path.exists():
        return []
    with path.open(newline="", encoding="utf-8") as f:
        return list(csv.DictReader(f))


def _append(path: Path, fields: list[str], rows: list[dict]) -> None:
    if not rows:
        return
    path.parent.mkdir(parents=True, exist_ok=True)
    new = not path.exists() or path.stat().st_size == 0
    with path.open("a", newline="", encoding="utf-8") as f:
        w = csv.DictWriter(f, fieldnames=fields, lineterminator="\n")
        if new:
            w.writeheader()
        w.writerows(rows)


def read_prices(path: Path) -> list[dict]:
    return _read(path)


def read_runs(path: Path) -> list[dict]:
    return _read(path)


def append_prices(path: Path, rows: list[dict]) -> None:
    _append(path, PRICE_FIELDS, rows)


def append_run(path: Path, row: dict) -> None:
    _append(path, RUN_FIELDS, [row])


def last_state(rows: list[dict]) -> dict:
    """(origin, dest, day) -> (dep_time, price, status) z ostatniego wiersza (plik jest chronologiczny)."""
    return {(r["origin"], r["dest"], r["day"]): (r["dep_time"], r["price"], r["status"]) for r in rows}


def load_routes(path: Path) -> list[str]:
    return json.loads(path.read_text(encoding="utf-8")) if path.exists() else []


def save_routes(path: Path, routes: list[str]) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(sorted(routes)) + "\n", encoding="utf-8")
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /home/areq/lcj-fares && .venv/bin/pytest tests/test_store.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd /home/areq/lcj-fares && git add lcjfares/store.py tests/test_store.py && git commit -m "feat: CSV change-log store"
```

---

### Task 3: Zbieranie (`collect`)

**Files:**
- Create: `lcjfares/collect.py`
- Test: `tests/test_collect.py`

**Interfaces:**
- Consumes: `ryanair.routes_from`, `ryanair.cheapest_per_day`, `ryanair.ApiError`, `ryanair.Fare` (Task 1); wszystkie funkcje `store` (Task 2). Wywołania **przez atrybut modułu** (`ryanair.cheapest_per_day(...)`), żeby testy mogły je podmienić `monkeypatch`.
- Produces:
  - `HOME = "LCJ"`, `MONTHS = 12`
  - `months_ahead(today: date, n: int = MONTHS) -> list[str]` — `["YYYY-MM-01", ...]`
  - `directions(routes: list[str]) -> list[tuple[str, str]]`
  - `diff_rows(snapshot: list[tuple[str, str, Fare]], state: dict, observed: str) -> list[dict]` — mutuje `state`
  - `run(data_dir: Path, now: datetime, *, pause=(0.5, 1.0), sleep=time.sleep) -> dict` — zwraca wiersz `runs.csv`
  - `main(argv: list[str] | None = None) -> int` — `python -m lcjfares.collect [data_dir]`, 1 gdy `failed`

- [ ] **Step 1: Write the failing tests**

`tests/test_collect.py`:
```python
import calendar
import datetime as dt

import pytest

from lcjfares import collect, ryanair, store
from lcjfares.ryanair import Fare

NOW = dt.datetime(2026, 9, 25, 6, 0, tzinfo=dt.timezone.utc)
NOSLEEP = {"pause": (0, 0), "sleep": lambda s: None}


def fake_api(monkeypatch, prices=None, routes=("STN",), fail=(), fail_all=False):
    """prices: {(origin, dest, day): "123.00"}; fail: {(origin, dest, "YYYY-MM")}."""
    prices = prices or {}

    def month(origin, dest, month, **kw):
        if fail_all or (origin, dest, month[:7]) in fail:
            raise ryanair.ApiError("boom")
        y, m = int(month[:4]), int(month[5:7])
        out = []
        for d in range(1, calendar.monthrange(y, m)[1] + 1):
            day = f"{y:04d}-{m:02d}-{d:02d}"
            p = prices.get((origin, dest, day))
            out.append(Fare(day, "10:00", p, "ok") if p else Fare(day, "", "", "unavailable"))
        return out

    monkeypatch.setattr(ryanair, "routes_from", lambda origin, **kw: list(routes))
    monkeypatch.setattr(ryanair, "cheapest_per_day", month)


def test_months_ahead():
    m = collect.months_ahead(dt.date(2026, 9, 25))
    assert len(m) == 12 and m[0] == "2026-09-01" and m[-1] == "2027-08-01"
    assert collect.months_ahead(dt.date(2026, 12, 3), 2) == ["2026-12-01", "2027-01-01"]


def test_directions_both_ways():
    assert collect.directions(["STN", "DUB"]) == [
        ("LCJ", "DUB"), ("DUB", "LCJ"), ("LCJ", "STN"), ("STN", "LCJ")]


def test_first_run_writes_future_days_only(tmp_path, monkeypatch):
    fake_api(monkeypatch, {("LCJ", "STN", "2026-10-10"): "199.00"})
    r = collect.run(tmp_path, NOW, **NOSLEEP)
    rows = store.read_prices(tmp_path / "prices.csv")
    days = (dt.date(2027, 8, 31) - dt.date(2026, 9, 25)).days + 1
    assert len(rows) == 2 * days
    assert min(x["day"] for x in rows) == "2026-09-25"
    assert {"observed": "2026-09-25", "origin": "LCJ", "dest": "STN", "day": "2026-10-10",
            "dep_time": "10:00", "price": "199.00", "status": "ok"} in rows
    assert r["status"] == "ok" and r["requests"] == 24 and r["failed"] == 0
    assert r["routes"] == "STN" and r["missing"] == ""
    assert store.load_routes(tmp_path / "routes.json") == ["STN"]


def test_rerun_same_day_adds_no_price_rows(tmp_path, monkeypatch):
    fake_api(monkeypatch, {("LCJ", "STN", "2026-10-10"): "199.00"})
    collect.run(tmp_path, NOW, **NOSLEEP)
    n = len(store.read_prices(tmp_path / "prices.csv"))
    collect.run(tmp_path, NOW, **NOSLEEP)
    assert len(store.read_prices(tmp_path / "prices.csv")) == n
    assert len(store.read_runs(tmp_path / "runs.csv")) == 2


def test_next_day_appends_only_changes(tmp_path, monkeypatch):
    fake_api(monkeypatch, {("LCJ", "STN", "2026-10-10"): "199.00"})
    collect.run(tmp_path, NOW, **NOSLEEP)
    n = len(store.read_prices(tmp_path / "prices.csv"))
    fake_api(monkeypatch, {("LCJ", "STN", "2026-10-10"): "149.00"})
    collect.run(tmp_path, NOW + dt.timedelta(days=1), **NOSLEEP)
    rows = store.read_prices(tmp_path / "prices.csv")
    assert len(rows) == n + 1
    assert rows[-1]["observed"] == "2026-09-26" and rows[-1]["price"] == "149.00"


def test_partial_failure_recorded(tmp_path, monkeypatch):
    fake_api(monkeypatch, fail={("LCJ", "STN", "2026-11")})
    r = collect.run(tmp_path, NOW, **NOSLEEP)
    assert r["status"] == "partial" and r["failed"] == 1
    assert r["missing"] == "LCJ-STN-2026-11"
    rows = store.read_prices(tmp_path / "prices.csv")
    assert not [x for x in rows if x["origin"] == "LCJ" and x["day"].startswith("2026-11")]
    assert [x for x in rows if x["origin"] == "STN" and x["day"].startswith("2026-11")]


_real_run = collect.run


def test_majority_failure_is_failed_and_exit_1(tmp_path, monkeypatch):
    fake_api(monkeypatch, fail_all=True)
    monkeypatch.setattr(collect, "run", lambda d, now: _real_run(d, now, **NOSLEEP))
    assert collect.main([str(tmp_path)]) == 1
    runs = store.read_runs(tmp_path / "runs.csv")
    assert runs[-1]["status"] == "failed" and runs[-1]["failed"] == "24"


def test_routes_fallback_to_saved_list(tmp_path, monkeypatch):
    fake_api(monkeypatch, routes=("STN", "DUB"))
    collect.run(tmp_path, NOW, **NOSLEEP)

    def boom(origin, **kw):
        raise ryanair.ApiError("down")

    monkeypatch.setattr(ryanair, "routes_from", boom)
    r = collect.run(tmp_path, NOW + dt.timedelta(days=1), **NOSLEEP)
    assert r["status"] == "ok" and r["routes"] == "DUB;STN" and r["requests"] == 48


def test_no_routes_at_all_is_failed(tmp_path, monkeypatch):
    fake_api(monkeypatch, routes=())
    r = collect.run(tmp_path, NOW, **NOSLEEP)
    assert r["status"] == "failed" and r["requests"] == 0
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /home/areq/lcj-fares && .venv/bin/pytest tests/test_collect.py -q`
Expected: FAIL — `ImportError: cannot import name 'collect'`

- [ ] **Step 3: Implement `lcjfares/collect.py`**

```python
"""Codzienny zbiór: kalendarz cheapestPerDay dla wszystkich kierunków z/do LCJ → zmiany w data/prices.csv."""
from __future__ import annotations

import datetime as dt
import random
import sys
import time
from pathlib import Path

from lcjfares import ryanair, store

HOME = "LCJ"
MONTHS = 12


def months_ahead(today: dt.date, n: int = MONTHS) -> list[str]:
    y, m, out = today.year, today.month, []
    for _ in range(n):
        out.append(f"{y:04d}-{m:02d}-01")
        y, m = (y + 1, 1) if m == 12 else (y, m + 1)
    return out


def directions(routes: list[str]) -> list[tuple[str, str]]:
    out = []
    for d in sorted(routes):
        out += [(HOME, d), (d, HOME)]
    return out


def diff_rows(snapshot, state: dict, observed: str) -> list[dict]:
    """Wiersze tylko dla kluczy nowych albo ze zmienioną (dep_time, price, status). Aktualizuje `state`."""
    rows = []
    for origin, dest, fare in snapshot:
        key = (origin, dest, fare.day)
        val = (fare.dep_time, fare.price, fare.status)
        if state.get(key) != val:
            rows.append({"observed": observed, "origin": origin, "dest": dest, "day": fare.day,
                         "dep_time": fare.dep_time, "price": fare.price, "status": fare.status})
            state[key] = val
    return rows


def _routes(routes_path: Path, sleep) -> list[str]:
    try:
        routes = ryanair.routes_from(HOME, sleep=sleep)
        if not routes:
            raise ryanair.ApiError("pusta lista tras")
    except ryanair.ApiError as e:
        print(f"trasy z API niedostępne ({e}) — używam zapisanej listy", file=sys.stderr)
        return store.load_routes(routes_path)
    store.save_routes(routes_path, routes)
    return routes


def run(data_dir: Path, now: dt.datetime, *, pause=(0.5, 1.0), sleep=time.sleep) -> dict:
    today = now.date()
    observed = today.isoformat()
    prices_path = data_dir / "prices.csv"
    routes = _routes(data_dir / "routes.json", sleep)
    state = store.last_state(store.read_prices(prices_path))

    snapshot, missing, requests = [], [], 0
    for origin, dest in directions(routes):
        for month in months_ahead(today):
            requests += 1
            try:
                fares = ryanair.cheapest_per_day(origin, dest, month, sleep=sleep)
            except ryanair.ApiError as e:
                print(f"{origin}-{dest} {month[:7]}: {e}", file=sys.stderr)
                missing.append(f"{origin}-{dest}-{month[:7]}")
            else:
                # API zwraca też minione dni bieżącego miesiąca (jako unavailable) — pomijamy
                snapshot += [(origin, dest, f) for f in fares
                             if f.day >= observed and f.day[:7] == month[:7]]
            sleep(random.uniform(*pause))

    store.append_prices(prices_path, diff_rows(snapshot, state, observed))
    failed = len(missing)
    status = "failed" if requests == 0 or failed * 2 > requests else "partial" if failed else "ok"
    run_row = {"observed": observed, "started_utc": now.strftime("%Y-%m-%dT%H:%M:%SZ"),
               "requests": requests, "failed": failed, "status": status,
               "routes": ";".join(sorted(routes)), "missing": ";".join(missing)}
    store.append_run(data_dir / "runs.csv", run_row)
    return run_row


def main(argv: list[str] | None = None) -> int:
    argv = sys.argv[1:] if argv is None else argv
    data_dir = Path(argv[0] if argv else "data")
    r = run(data_dir, dt.datetime.now(dt.timezone.utc))
    print(f"{r['status']}: {r['requests']} zapytań, {r['failed']} nieudanych")
    return 1 if r["status"] == "failed" else 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /home/areq/lcj-fares && .venv/bin/pytest tests/test_collect.py -q`
Expected: PASS. (Uwaga dla `test_majority_failure_is_failed_and_exit_1`: `main` woła `run(data_dir, now)` przez globalną nazwę modułu, więc podmiana `collect.run` działa; `_real_run` to oryginał zapamiętany na poziomie modułu testu.)

- [ ] **Step 5: Commit**

```bash
cd /home/areq/lcj-fares && git add lcjfares/collect.py tests/test_collect.py && git commit -m "feat: daily collector appending only price changes"
```

---

### Task 4: Analiza (`analyze`)

**Files:**
- Create: `lcjfares/analyze.py`
- Test: `tests/test_analyze.py`

**Interfaces:**
- Consumes: `store.read_prices`, `store.read_runs`, `store.last_state` (Task 2). Format wierszy `prices.csv`/`runs.csv` z Global Constraints.
- Produces:
  - `MIN_N = 5`, `BUCKETS`
  - `bucket_label(days: int) -> str | None` — np. `"0-7"`, `"181+"`, `None` dla ujemnych
  - `reconstruct(prices: list[dict], runs: list[dict]) -> list[dict]` — obserwacje `{"origin", "dest", "day", "observed", "price": float | None, "status"}`
  - `build(prices: list[dict], runs: list[dict], today: date) -> dict` — struktura `data.json`:
    ```
    {"generated": "YYYY-MM-DD", "last_ok": "YYYY-MM-DD"|null, "history_days": int, "min_n": 5,
     "routes": {"LCJ-STN": {
        "normal":   {"median", "q1", "q3", "n"},
        "heatmap":  [{"month": "YYYY-MM", "weekday": 0..6, "median", "n"}],
        "curve":    [{"bucket": "31-60", "median", "ratio", "n"}],     # kolejność jak BUCKETS
        "cheapest": [{"day", "dep_time", "price", "vs_median_pct"}]}}}  # max 10
    ```
  - `main(argv) -> int` — `python -m lcjfares.analyze [data_dir] [out.json]`

- [ ] **Step 1: Write the failing tests**

`tests/test_analyze.py`:
```python
import datetime as dt
import json

import pytest

from lcjfares import analyze, store


def p(observed, day, price, status="ok", dep="10:00", origin="LCJ", dest="STN"):
    return {"observed": observed, "origin": origin, "dest": dest, "day": day,
            "dep_time": dep, "price": price, "status": status}


def run(observed, routes="STN", missing="", status="ok"):
    return {"observed": observed, "started_utc": observed + "T06:00:00Z", "requests": "24",
            "failed": "0", "status": status, "routes": routes, "missing": missing}


@pytest.mark.parametrize("days,label", [
    (0, "0-7"), (7, "0-7"), (8, "8-14"), (30, "15-30"), (31, "31-60"),
    (90, "61-90"), (180, "91-180"), (181, "181+"), (400, "181+"), (-1, None)])
def test_bucket_label(days, label):
    assert analyze.bucket_label(days) == label


def test_reconstruct_carries_forward_and_respects_missing():
    prices = [p("2026-09-01", "2026-10-10", "100.00"), p("2026-09-03", "2026-10-10", "150.00")]
    runs = [run("2026-09-01"), run("2026-09-02"), run("2026-09-03"),
            run("2026-09-04", missing="LCJ-STN-2026-10", status="partial"), run("2026-09-05")]
    obs = analyze.reconstruct(prices, runs)
    assert [(o["observed"], o["price"]) for o in obs] == [
        ("2026-09-01", 100.0), ("2026-09-02", 100.0), ("2026-09-03", 150.0), ("2026-09-05", 150.0)]


def test_reconstruct_skips_route_not_attempted():
    prices = [p("2026-09-01", "2026-10-10", "100.00")]
    runs = [run("2026-09-01"), run("2026-09-02", routes="DUB")]
    assert [o["observed"] for o in analyze.reconstruct(prices, runs)] == ["2026-09-01"]


def test_reconstruct_stops_at_flight_day_and_before_first_row():
    prices = [p("2026-09-02", "2026-09-03", "100.00")]
    runs = [run("2026-09-01"), run("2026-09-02"), run("2026-09-03"), run("2026-09-04")]
    assert [o["observed"] for o in analyze.reconstruct(prices, runs)] == ["2026-09-02", "2026-09-03"]


def test_reconstruct_soldout_has_no_price():
    prices = [p("2026-09-01", "2026-10-10", "100.00"), p("2026-09-02", "2026-10-10", "", "soldout")]
    obs = analyze.reconstruct(prices, [run("2026-09-01"), run("2026-09-02")])
    assert [(o["price"], o["status"]) for o in obs] == [(100.0, "ok"), (None, "soldout")]


def test_build_empty():
    out = analyze.build([], [], dt.date(2026, 9, 25))
    assert out == {"generated": "2026-09-25", "last_ok": None, "history_days": 0,
                   "min_n": 5, "routes": {}}


def test_build_full():
    prices = [
        p("2026-09-01", "2026-10-06", "200.00"),
        p("2026-09-04", "2026-10-06", "100.00"),
        p("2026-09-01", "2026-10-07", "300.00"),
        p("2026-09-01", "2026-10-08", "50.00"),
        p("2026-09-03", "2026-10-08", "", "soldout", dep=""),
    ]
    runs = [run(f"2026-09-0{d}") for d in range(1, 6)] + [run("2026-09-06", status="failed", routes="")]
    out = analyze.build(prices, runs, dt.date(2026, 9, 5))
    assert out["last_ok"] == "2026-09-05" and out["history_days"] == 5
    r = out["routes"]["LCJ-STN"]
    assert r["normal"] == {"median": 200.0, "q1": 150.0, "q3": 250.0, "n": 2}
    assert r["cheapest"] == [
        {"day": "2026-10-06", "dep_time": "10:00", "price": 100.0, "vs_median_pct": -50},
        {"day": "2026-10-07", "dep_time": "10:00", "price": 300.0, "vs_median_pct": 50},
    ]
    heat = {(c["month"], c["weekday"]): c for c in r["heatmap"]}
    assert len(heat) == 3
    assert heat[("2026-10", dt.date(2026, 10, 6).weekday())] == {
        "month": "2026-10", "weekday": dt.date(2026, 10, 6).weekday(), "median": 100.0, "n": 1}
    assert heat[("2026-10", dt.date(2026, 10, 8).weekday())]["median"] == 50.0
    assert r["curve"] == [{"bucket": "31-60", "median": 200.0, "ratio": 1.0, "n": 12}]


def test_main_writes_json(tmp_path):
    store.append_prices(tmp_path / "prices.csv", [p("2026-09-01", "2030-01-01", "100.00")])
    store.append_run(tmp_path / "runs.csv", run("2026-09-01"))
    out = tmp_path / "site" / "data.json"
    assert analyze.main([str(tmp_path), str(out)]) == 0
    data = json.loads(out.read_text())
    assert "LCJ-STN" in data["routes"]
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /home/areq/lcj-fares && .venv/bin/pytest tests/test_analyze.py -q`
Expected: FAIL — `ImportError: cannot import name 'analyze'`

- [ ] **Step 3: Implement `lcjfares/analyze.py`**

```python
"""Historia cen → agregaty dla strony (site/data.json)."""
from __future__ import annotations

import datetime as dt
import json
import statistics
import sys
from bisect import bisect_right
from collections import defaultdict
from pathlib import Path

from lcjfares import store

MIN_N = 5
HORIZON_MONTHS = 12
BUCKETS = [(0, 7), (8, 14), (15, 30), (31, 60), (61, 90), (91, 180), (181, None)]
LABELS = [f"{lo}+" if hi is None else f"{lo}-{hi}" for lo, hi in BUCKETS]


def _date(s: str) -> dt.date:
    return dt.date.fromisoformat(s)


def _month_index(s: str) -> int:
    return int(s[:4]) * 12 + int(s[5:7])


def bucket_label(days: int) -> str | None:
    for (lo, hi), label in zip(BUCKETS, LABELS):
        if days >= lo and (hi is None or days <= hi):
            return label
    return None


def _split(s: str) -> set[str]:
    return set(filter(None, s.split(";")))


def reconstruct(prices: list[dict], runs: list[dict]) -> list[dict]:
    """Stan każdego lotu w każdym dniu, w którym był faktycznie mierzony (luki pomijane)."""
    hist = defaultdict(list)
    for r in prices:
        hist[(r["origin"], r["dest"], r["day"])].append(r)
    coverage = defaultdict(list)  # observed -> [(routes, missing)]
    for r in runs:
        coverage[r["observed"]].append((_split(r["routes"]), _split(r["missing"])))
    run_days = sorted(coverage)

    obs = []
    for (origin, dest, day), rows in hist.items():
        dates = [r["observed"] for r in rows]
        month_key = f"{origin}-{dest}-{day[:7]}"
        for d in run_days:
            if d < dates[0] or d > day or _month_index(day) - _month_index(d) >= HORIZON_MONTHS:
                continue
            if not any((origin in routes or dest in routes) and month_key not in missing
                       for routes, missing in coverage[d]):
                continue
            row = rows[bisect_right(dates, d) - 1]
            obs.append({"origin": origin, "dest": dest, "day": day, "observed": d,
                        "price": float(row["price"]) if row["price"] else None,
                        "status": row["status"]})
    return obs


def _median(vals):
    return round(statistics.median(vals), 2) if vals else None


def _stats(vals):
    if not vals:
        return {"median": None, "q1": None, "q3": None, "n": 0}
    q1 = q3 = vals[0]
    if len(vals) > 1:
        q1, _, q3 = statistics.quantiles(vals, n=4, method="inclusive")
    return {"median": _median(vals), "q1": round(q1, 2), "q3": round(q3, 2), "n": len(vals)}


def build(prices: list[dict], runs: list[dict], today: dt.date) -> dict:
    t = today.isoformat()
    state = store.last_state(prices)
    last_priced = {}  # ostatnia znana cena lotu (także lotów już odbytych / wyprzedanych)
    for r in prices:
        if r["price"]:
            last_priced[(r["origin"], r["dest"], r["day"])] = r
    obs = [o for o in reconstruct(prices, runs) if o["price"] is not None]
    min_price = {}
    for o in obs:
        k = (o["origin"], o["dest"], o["day"])
        min_price[k] = min(min_price.get(k, o["price"]), o["price"])

    routes = defaultdict(lambda: {"heat": defaultdict(list), "upcoming": [],
                                  "curve": defaultdict(lambda: ([], []))})
    for (origin, dest, day), r in last_priced.items():
        routes[f"{origin}-{dest}"]["heat"][(day[:7], _date(day).weekday())].append(float(r["price"]))
    for (origin, dest, day), (dep, price, status) in state.items():
        if status == "ok" and price and day >= t:
            routes[f"{origin}-{dest}"]["upcoming"].append((day, dep, float(price)))
    for o in obs:
        label = bucket_label((_date(o["day"]) - _date(o["observed"])).days)
        if label is None:
            continue
        ps, ratios = routes[f"{o['origin']}-{o['dest']}"]["curve"][label]
        ps.append(o["price"])
        ratios.append(o["price"] / min_price[(o["origin"], o["dest"], o["day"])])

    out = {}
    for name in sorted(routes):
        r = routes[name]
        normal = _stats([p for _, _, p in r["upcoming"]])
        med = normal["median"]
        out[name] = {
            "normal": normal,
            "heatmap": [{"month": m, "weekday": wd, "median": _median(v), "n": len(v)}
                        for (m, wd), v in sorted(r["heat"].items())],
            "curve": [{"bucket": lb, "median": _median(r["curve"][lb][0]),
                       "ratio": round(statistics.median(r["curve"][lb][1]), 3),
                       "n": len(r["curve"][lb][0])}
                      for lb in LABELS if lb in r["curve"]],
            "cheapest": [{"day": d, "dep_time": dep, "price": p,
                          "vs_median_pct": round((p / med - 1) * 100) if med else None}
                         for d, dep, p in sorted(r["upcoming"], key=lambda x: (x[2], x[0]))[:10]],
        }
    ok_days = sorted({r["observed"] for r in runs if r["status"] != "failed"})
    return {"generated": t, "last_ok": ok_days[-1] if ok_days else None,
            "history_days": len(ok_days), "min_n": MIN_N, "routes": out}


def main(argv: list[str] | None = None) -> int:
    argv = sys.argv[1:] if argv is None else argv
    data_dir = Path(argv[0] if argv else "data")
    out_path = Path(argv[1] if len(argv) > 1 else "site/data.json")
    data = build(store.read_prices(data_dir / "prices.csv"), store.read_runs(data_dir / "runs.csv"),
                 dt.datetime.now(dt.timezone.utc).date())
    out_path.parent.mkdir(parents=True, exist_ok=True)
    out_path.write_text(json.dumps(data, ensure_ascii=False, indent=1) + "\n", encoding="utf-8")
    print(f"{out_path}: {len(data['routes'])} kierunków, {data['history_days']} dni historii")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

Weryfikacja liczb w `test_build_full` (do sanity-checku, gdyby test nie przechodził):
- aktualny stan: 10-06 ok 100, 10-07 ok 300, 10-08 soldout ⇒ `upcoming = [100, 300]`, mediana 200, kwartyle (inclusive) 150/250.
- krzywa: 10-06 → 200,200,200,100,100 (dni do wylotu 35..31); 10-07 → 300×5; 10-08 → 50,50 (09-01, 09-02; od 09-03 soldout bez ceny). Wszystko w koszyku `31-60`, n=12, mediana cen 200; ilorazy do minimum lotu: dziewięć 1.0 i trzy 2.0 ⇒ mediana 1.0.
- run 09-06 ma `status=failed` i puste `routes` ⇒ nie liczy się do `history_days` i niczego nie mierzy.

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /home/areq/lcj-fares && .venv/bin/pytest -q`
Expected: PASS (cały zestaw, Taski 1–4)

- [ ] **Step 5: Commit**

```bash
cd /home/areq/lcj-fares && git add lcjfares/analyze.py tests/test_analyze.py && git commit -m "feat: price history analysis (heatmap, buy-timing curve, cheapest)"
```

---

### Task 5: Strona (`site/index.html`)

**Files:**
- Create: `site/index.html`

**Interfaces:**
- Consumes: `site/data.json` o strukturze z Task 4 (`build`).
- Produces: statyczna strona dla GitHub Pages.

- [ ] **Step 1: Napisz `site/index.html`**

```html
<!doctype html>
<html lang="pl">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Tanie loty z Łodzi</title>
<style>
:root { --bg:#fafaf7; --fg:#1d1d1b; --muted:#6b6b66; --line:#e2e1dc; --accent:#1f6feb; }
@media (prefers-color-scheme: dark) {
  :root { --bg:#161615; --fg:#ecebe6; --muted:#9a9993; --line:#33332f; --accent:#6ea8ff; }
}
body { margin:0; background:var(--bg); color:var(--fg); font:15px/1.5 system-ui, sans-serif; }
main { max-width:960px; margin:0 auto; padding:16px; }
h1 { font-size:1.4rem; margin:.2em 0; }
h2 { font-size:1.1rem; margin:0 0 .3em; }
.muted { color:var(--muted); font-size:.9rem; margin:.2em 0 .6em; }
select { font:inherit; padding:.3em .5em; margin:.8em 0; }
section { margin:1.8em 0; }
.scroll { overflow-x:auto; }
table { border-collapse:collapse; font-size:.85rem; }
th, td { padding:.3em .5em; text-align:right; border-bottom:1px solid var(--line); white-space:nowrap; }
th:first-child, td:first-child { text-align:left; }
td.cell { color:#111; }
.few { opacity:.4; }
.chart { position:relative; height:240px; }
</style>
</head>
<body>
<main>
  <h1>Tanie loty z/do Łodzi (Ryanair)</h1>
  <p class="muted" id="meta">Ładowanie…</p>
  <label>Kierunek: <select id="route"></select></label>

  <section>
    <h2>Kiedy tanio: dzień tygodnia × miesiąc</h2>
    <p class="muted">Mediana ostatniej znanej ceny lotów. Wyszarzone: mniej niż <span class="minn"></span> lotów.</p>
    <div class="scroll"><table id="heat"></table></div>
  </section>

  <section>
    <h2>Kiedy kupować</h2>
    <p class="muted">Mediana ceny wg liczby dni do wylotu. „× min” — ile razy drożej od najniższej ceny, jaką dany lot kiedykolwiek miał.</p>
    <div class="chart"><canvas id="curve"></canvas></div>
    <div class="scroll"><table id="curveTable"></table></div>
  </section>

  <section>
    <h2>Najtańsze nadchodzące loty</h2>
    <p class="muted" id="normal"></p>
    <div class="scroll"><table id="cheap"></table></div>
  </section>
</main>
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>
<script>
const WD = ["Pn", "Wt", "Śr", "Cz", "Pt", "So", "Nd"];
const zl = v => v == null ? "–" : Math.round(v) + " zł";
let DATA, chart;

function el(tag, text, cls) {
  const e = document.createElement(tag);
  if (text != null) e.textContent = text;
  if (cls) e.className = cls;
  return e;
}
function row(cells, tag = "td") {
  const tr = el("tr");
  cells.forEach(c => tr.append(c instanceof Node ? c : el(tag, c)));
  return tr;
}
function color(v, lo, hi) {
  const t = hi > lo ? (v - lo) / (hi - lo) : 0;
  return `hsl(${120 - 120 * t} 60% 72%)`;
}

function renderHeat(r) {
  const t = document.getElementById("heat");
  t.replaceChildren();
  const months = [...new Set(r.heatmap.map(c => c.month))].sort();
  if (!months.length) { t.append(el("caption", "Brak danych")); return; }
  const vals = r.heatmap.map(c => c.median);
  const lo = Math.min(...vals), hi = Math.max(...vals);
  t.append(row(["", ...months], "th"));
  WD.forEach((name, wd) => {
    const cells = [el("th", name)];
    months.forEach(m => {
      const c = r.heatmap.find(x => x.month === m && x.weekday === wd);
      const td = el("td", c ? zl(c.median) : "");
      if (c) {
        td.className = "cell" + (c.n < DATA.min_n ? " few" : "");
        td.style.background = color(c.median, lo, hi);
        td.title = `n=${c.n}`;
      }
      cells.push(td);
    });
    t.append(row(cells));
  });
}

function renderCurve(r) {
  const t = document.getElementById("curveTable");
  t.replaceChildren(row(["dni do wylotu", "mediana", "× min", "n"], "th"));
  r.curve.forEach(c => {
    const tr = row([c.bucket, zl(c.median), "×" + c.ratio.toFixed(2), String(c.n)]);
    if (c.n < DATA.min_n) tr.className = "few";
    t.append(tr);
  });
  if (!window.Chart) return;
  if (chart) chart.destroy();
  const accent = getComputedStyle(document.documentElement).getPropertyValue("--accent").trim();
  chart = new Chart(document.getElementById("curve"), {
    type: "line",
    data: {
      labels: r.curve.map(c => c.bucket),
      datasets: [{ data: r.curve.map(c => c.median), borderColor: accent, backgroundColor: accent, tension: .3 }],
    },
    options: {
      maintainAspectRatio: false,
      plugins: { legend: { display: false },
                 tooltip: { callbacks: { label: ctx => `${zl(ctx.raw)} (n=${r.curve[ctx.dataIndex].n})` } } },
      scales: { x: { reverse: true, title: { display: true, text: "dni do wylotu" } },
                y: { title: { display: true, text: "zł" } } },
    },
  });
}

function renderCheap(r) {
  const n = r.normal;
  document.getElementById("normal").textContent = n.n
    ? `Cena normalna (mediana nadchodzących): ${zl(n.median)}, typowo ${zl(n.q1)}–${zl(n.q3)}, lotów: ${n.n}.`
    : "Brak nadchodzących lotów z ceną.";
  const t = document.getElementById("cheap");
  t.replaceChildren(row(["data", "dzień", "godz.", "cena", "vs norma"], "th"));
  r.cheapest.forEach(c => {
    const wd = WD[(new Date(c.day + "T12:00:00").getDay() + 6) % 7];
    const pct = c.vs_median_pct == null ? "–" : (c.vs_median_pct > 0 ? "+" : "") + c.vs_median_pct + "%";
    t.append(row([c.day, wd, c.dep_time, zl(c.price), pct]));
  });
}

function render() {
  const r = DATA.routes[document.getElementById("route").value];
  renderHeat(r); renderCurve(r); renderCheap(r);
}

fetch("data.json", { cache: "no-store" })
  .then(res => res.json())
  .then(data => {
    DATA = data;
    document.querySelectorAll(".minn").forEach(e => e.textContent = data.min_n);
    document.getElementById("meta").textContent =
      `Ostatni udany pomiar: ${data.last_ok ?? "brak"} · dni historii: ${data.history_days}`;
    const sel = document.getElementById("route");
    const names = Object.keys(data.routes);
    if (!names.length) { document.getElementById("meta").textContent += " · brak danych jeszcze"; return; }
    names.forEach(n => sel.append(new Option(n.replace("-", " → "), n)));
    sel.value = names.find(n => n.startsWith("LCJ-")) ?? names[0];
    sel.addEventListener("change", render);
    render();
  })
  .catch(e => { document.getElementById("meta").textContent = "Nie udało się wczytać danych: " + e; });
</script>
</body>
</html>
```

- [ ] **Step 2: Weryfikacja ręczna na danych testowych**

Run:
```bash
cd /home/areq/lcj-fares && .venv/bin/python -c "
import datetime as dt, json
from lcjfares import analyze
P=lambda o,d,pr,s='ok':{'observed':o,'origin':'LCJ','dest':'STN','day':d,'dep_time':'10:00','price':pr,'status':s}
R=lambda o:{'observed':o,'started_utc':'','requests':'24','failed':'0','status':'ok','routes':'STN','missing':''}
prices=[P('2026-09-01','2026-10-06','200.00'),P('2026-09-04','2026-10-06','100.00'),P('2026-09-01','2026-10-07','300.00')]
open('site/data.json','w').write(json.dumps(analyze.build(prices,[R(f'2026-09-0{i}') for i in range(1,6)],dt.date(2026,9,5))))"
.venv/bin/python -m http.server -d site 8765
```
Otwórz `http://localhost:8765/` — expected: widoczny kierunek „LCJ → STN”, heatmapa z 2 komórkami, wykres z punktem `31-60`, tabela 2 lotów. Potem nadpisz `site/data.json` treścią `{"generated":"2026-09-25","last_ok":null,"history_days":0,"min_n":5,"routes":{}}`, odśwież — expected: „brak danych jeszcze”, brak błędów w konsoli. Zatrzymaj serwer, usuń `site/data.json` (`rm site/data.json`).

- [ ] **Step 3: Commit**

```bash
cd /home/areq/lcj-fares && git add site/index.html && git commit -m "feat: static report page (heatmap, buy-timing chart, cheapest flights)"
```

---

### Task 6: Workflow GitHub Actions, README, pierwszy prawdziwy zbiór lokalnie

**Files:**
- Create: `.github/workflows/collect.yml`, `README.md`
- Create (wynik uruchomienia): `data/prices.csv`, `data/runs.csv`, `data/routes.json`, `site/data.json`

**Interfaces:**
- Consumes: `python -m lcjfares.collect data`, `python -m lcjfares.analyze data site/data.json` (Taski 3–4), `site/` (Task 5).
- Produces: działający workflow.

- [ ] **Step 1: Napisz `.github/workflows/collect.yml`**

```yaml
name: collect

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: collect
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: pytest -q
      - id: collect
        run: python -m lcjfares.collect data
        continue-on-error: true
      - run: python -m lcjfares.analyze data site/data.json
      - name: Commit data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data site/data.json
          git diff --cached --quiet || git commit -m "data: $(date -u +%F)"
          git pull --rebase
          git push
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deploy
        uses: actions/deploy-pages@v4
      - name: Fail if collection failed
        if: steps.collect.outcome == 'failure'
        run: exit 1
```

- [ ] **Step 2: Napisz `README.md`**

```markdown
# lcj-fares

Codzienny monitoring cen Ryanair z/do Łodzi (LCJ) — historia cen i analiza „kiedy tanio / kiedy kupować”.

**Strona:** https://areqq.github.io/lcj-fares/

## Jak to działa
- GitHub Actions (`.github/workflows/collect.yml`) codziennie o 06:00 UTC pobiera kalendarz najtańszych cen
  (`cheapestPerDay`) dla wszystkich kierunków z/do LCJ na 12 miesięcy do przodu.
- `data/prices.csv` — dziennik zmian: wiersz tylko gdy cena/status lotu się zmienia.
- `data/runs.csv` — log uruchomień (`ok` / `partial` / `failed`, lista braków).
- `site/data.json` — agregaty dla strony (heatmapa, krzywa „kiedy kupować”, najtańsze loty).

Specyfikacja: `docs/superpowers/specs/2026-09-25-lcj-fares-design.md`.

## Lokalnie
```bash
python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
.venv/bin/pytest -q
.venv/bin/python -m lcjfares.collect data          # ~170 zapytań, 2–3 min
.venv/bin/python -m lcjfares.analyze data site/data.json
.venv/bin/python -m http.server -d site 8765       # http://localhost:8765/
```
```

- [ ] **Step 3: Pierwszy prawdziwy zbiór lokalnie**

Run:
```bash
cd /home/areq/lcj-fares && .venv/bin/python -m lcjfares.collect data && .venv/bin/python -m lcjfares.analyze data site/data.json && tail -1 data/runs.csv && wc -l data/prices.csv && cat data/routes.json
```
Expected: `ok: 168 zapytań, 0 nieudanych` (liczba = 14 × 12 przy 7 trasach), `site/data.json: 14 kierunków`, ~5000 wierszy w `prices.csv`, `["AGP", "ALC", "BGY", "BHX", "CRL", "DUB", "STN"]`. Jeśli status `partial` — sprawdź `missing` w `runs.csv` i komunikaty na stderr przed commitem.

- [ ] **Step 4: Obejrzyj stronę na prawdziwych danych**

Run: `cd /home/areq/lcj-fares && .venv/bin/python -m http.server -d site 8765`
Expected: 14 kierunków w liście; heatmapa wypełniona (jedna obserwacja na lot — wszystko wyszarzone tylko tam, gdzie n<5); krzywa ma dziś jeden punkt na koszyk (wszystkie loty zmierzone 1 raz); tabela najtańszych. Zatrzymaj serwer.

- [ ] **Step 5: Commit**

```bash
cd /home/areq/lcj-fares && .venv/bin/pytest -q && git add .github README.md data site/data.json && git commit -m "ci: daily collect + Pages deploy; first data snapshot"
```

---

### Task 7: Publikacja na GitHubie i smoke test z runnera

**Files:** brak zmian w kodzie (chyba że smoke test wykaże blokadę — wtedy STOP i powrót do użytkownika).

**Interfaces:**
- Consumes: gotowe repo z Tasków 1–6; `gh` zalogowane jako `areqq`.
- Produces: publiczne repo `areqq/lcj-fares`, aktywne Pages, udany run workflow.

- [ ] **Step 1: POTWIERDŹ z użytkownikiem utworzenie publicznego repo**

Zapytaj: „Tworzę publiczne repo `areqq/lcj-fares` i wypycham kod + dane — OK?” Nie wykonuj kroków 2–5 bez wyraźnego „tak”.

- [ ] **Step 2: Utwórz repo i wypchnij**

Run:
```bash
cd /home/areq/lcj-fares && git branch -M main && gh repo create areqq/lcj-fares --public --source . --push --description "Monitoring cen Ryanair z/do Łodzi"
```
Expected: URL `https://github.com/areqq/lcj-fares`.

- [ ] **Step 3: Włącz Pages w trybie GitHub Actions**

Run: `gh api -X POST repos/areqq/lcj-fares/pages -f build_type=workflow`
Expected: JSON z `"build_type": "workflow"` (albo 409, jeśli już włączone — OK).

- [ ] **Step 4: Ręczny run workflow (smoke test IP runnera)**

Run:
```bash
gh workflow run collect.yml -R areqq/lcj-fares && sleep 5 && gh run watch -R areqq/lcj-fares $(gh run list -R areqq/lcj-fares -w collect.yml -L 1 --json databaseId -q '.[0].databaseId') --exit-status
```
Expected: run zielony. Następnie:
```bash
gh run view -R areqq/lcj-fares --log $(gh run list -R areqq/lcj-fares -w collect.yml -L 1 --json databaseId -q '.[0].databaseId') | grep -E "(ok|partial|failed): .* zapytań"
```
Expected: `ok: 168 zapytań, 0 nieudanych`. **Jeśli `failed` z błędami 403/429** — Ryanair blokuje IP GitHuba: STOP, zgłoś użytkownikowi (spec: powrót do decyzji o miejscu uruchamiania; nie wyłączaj workflow bez pytania).

- [ ] **Step 5: Sprawdź stronę**

Run: `curl -s https://areqq.github.io/lcj-fares/data.json | head -c 300`
Expected: JSON z `"history_days"` ≥ 1 i niepustym `"routes"`. Otwórz `https://areqq.github.io/lcj-fares/` i przejrzyj widoki.
